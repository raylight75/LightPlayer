namespace Player.Helpers
{
    public enum PlaybackStates
    {
        Playing, Stopped, Paused, Starting, Changed
    }

    public enum PlaybackSource
    {
        Stream, Path, Playlist, Youtube
    }
}
